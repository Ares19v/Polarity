version: '3.8'

services:
  polarity_daemon:
    build: .
    container_name: polarity_daemon
    ports:
      - "8000:8000"
    volumes:
      - ${PROJECTS_DIR:-..}:/projects
    environment:
      - PROJECTS_DIR=/projects
    restart: unless-stopped
